const raio = 5.6;
// Math.PI --> Indica valor do PI
// Math.pow --> valor elevado ao quadrado, cubo, a quarta potencia...;
const area = Math.PI * Math.pow(raio,2);

console.log(area);
console.log(typeof Math);